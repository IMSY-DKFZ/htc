[Warnings]
regex: \/.*?\S+
replace: FILE_PATH

[timeit]
regex: [^(]+per\s+loop\s+\(mean ± std\. dev\. of \d+ runs, \d+ loops? each\)
replace: TIMEIT_RESULT

[sample_print]
regex: Number of samples:\s+\d+.*
replace: SAMPLE_PRINT

[filename_logging]
regex: \w+\.py:\d+
replace: FILENAME_LOGGING

[numbers]
regex: (\d+\.\d\d\d\d)\d+\s*
replace: \g<1>NUMBER_DIGITS
